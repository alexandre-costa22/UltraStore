@* @model UltraStore.Models.Nota

@{
    ViewData["Title"] = "Detalhes da Nota Fiscal";
}

<h1>Detalhes da Nota Fiscal #@Model.Id</h1>

<div>
    <strong>Data da Compra:</strong> @Model.PurchaseDate.ToString("dd/MM/yyyy")
</div>
<div>
    <strong>Cliente:</strong> @Model.Clients.Name
</div>
<div>
    <strong>Vendedor:</strong> @Model.Seller.Name
</div>
<div>
    <strong>Jogos:</strong>
    <ul>
        @foreach (var game in Model.Games)
        {
            <li>@game.Name</li>
        }
    </ul>
</div>
<div>
    <strong>Total:</strong> @Model.TotalPrice.ToString("C")
</div>

<a href="@Url.Action("Index", "NF")" class="btn btn-secondary">Voltar</a>
 *@